{
  "id": "q-048",
  "subject": 2,
  "category": "JOIN",
  "type": "mc",
  "question": "다음 NATURAL JOIN에 대한 설명으로 옳지 않은 것은?",
  "choices": [
    "두 테이블에서 동일한 이름과 데이터 타입을 가진 컬럼을 자동으로 조인 조건으로 사용한다.",
    "조인 컬럼은 결과에서 한 번만 나타난다.",
    "ON 절을 명시적으로 사용해야 한다.",
    "공통 컬럼이 없으면 카티전 곱이 발생한다."
  ],
  "answer": 2,
  "explanation": "NATURAL JOIN은 **자동으로 동일 이름 컬럼**을 조인 조건으로 사용하므로 **ON 절을 쓸 수 없습니다**.\n\n```sql\nSELECT * FROM emp NATURAL JOIN dept;\n-- 자동: emp.deptno = dept.deptno (동일 이름 컬럼 사용)\n```\n\n특징:\n- 공통 컬럼은 결과에서 **한 번만** 출력 (USING과 유사)\n- 한정자 (`e.deptno`) 사용 불가\n- **공통 컬럼이 없으면 CROSS JOIN(카티전 곱)** 발생 → 위험!\n\n실무에서는 의도치 않은 매칭 위험으로 **잘 사용하지 않습니다**. 명시적 `JOIN ... ON` 권장.",
  "concepts": ["join-types"],
  "tags": ["NATURAL JOIN", "JOIN"],
  "source": "self"
}
