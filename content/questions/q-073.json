{
  "id": "q-073",
  "subject": 2,
  "category": "윈도우함수",
  "type": "mc",
  "question": "다음 윈도우 함수에서 PARTITION BY를 생략하면 어떻게 되는가?",
  "codeBlock": "SELECT ename, sal, RANK() OVER (ORDER BY sal DESC) AS r\nFROM emp;",
  "choices": [
    "에러가 발생한다.",
    "전체 데이터를 하나의 윈도우로 처리하여 순위를 매긴다.",
    "PARTITION BY가 없으면 RANK가 항상 1이 된다.",
    "각 행마다 별도의 윈도우가 생성된다."
  ],
  "answer": 1,
  "explanation": "**PARTITION BY 생략 = 전체 데이터를 하나의 윈도우**로 취급합니다.\n\n```sql\nRANK() OVER (ORDER BY sal DESC)\n-- 모든 사원을 sal 기준 순위\n\nRANK() OVER (PARTITION BY deptno ORDER BY sal DESC)\n-- 부서 안에서 sal 기준 순위 (부서마다 1위가 다시 시작)\n```\n\n윈도우 함수 구성:\n```\n함수() OVER (\n  [PARTITION BY ...]     -- 생략 시 전체\n  [ORDER BY ...]         -- 순위 함수에선 필수\n  [ROWS/RANGE BETWEEN ...] -- 집계 함수에서 사용\n)\n```",
  "concepts": ["window-functions"],
  "tags": ["윈도우 함수", "PARTITION BY"],
  "source": "self"
}
